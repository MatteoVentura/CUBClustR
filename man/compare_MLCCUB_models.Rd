% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CUBClustR_functions.R
\name{compare_MLCCUB_models}
\alias{compare_MLCCUB_models}
\title{Compare MLCCUB Models for Different Values of K}
\usage{
compare_MLCCUB_models(data, m, K_vector, ...)
}
\arguments{
\item{data}{A numeric matrix or data.frame containing the observed ordinal ratings.}

\item{m}{A numeric vector specifying the maximum number of categories for each item.}

\item{K_vector}{A numeric vector containing the different values of K to be tested (e.g., c(2, 3, 4)).}

\item{...}{Additional arguments passed to the \code{fit_MLCCUB} function.}
}
\value{
A list containing all the fitted models (\code{all_models}), a summary data.frame with LogLikelihood and BIC (\code{summary}), the best K value (\code{best_k}), and the best model object (\code{best_model}).
}
\description{
Fits multiple MLCCUB models for a sequence of latent classes (K) and compares them using the Bayesian Information Criterion (BIC).
}
\examples{
\dontrun{
set.seed(123)
data_mock <- matrix(sample(1:7, 100 * 3, replace = TRUE), ncol = 3)
m_vec <- rep(7, 3)

# Quick comparison among models with K = 1, 2, and 3
comparison <- compare_MLCCUB_models(data_mock, m_vec, K_vector = 1:3, EM_iter = 2)

# Print the summary with LogLik and BIC to choose the optimal model
print(comparison$summary)
}
}
