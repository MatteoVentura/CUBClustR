% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CUBClustR_functions.R
\name{plot_CUB_fit}
\alias{plot_CUB_fit}
\title{Minimal Plot of CUB Distributions (Observed vs Theoretical)}
\usage{
plot_CUB_fit(model_obj, data, m, cluster_order = NULL, line_color = "darkblue")
}
\arguments{
\item{model_obj}{The model object returned by \code{fit_MLCCUB}.}

\item{data}{The original dataset containing the observed responses.}

\item{m}{A numeric vector specifying the maximum number of categories for each item.}

\item{cluster_order}{Optional numeric vector to manually reorder the clusters.}

\item{line_color}{Color of the dashed theoretical probability line (default: "#008B8B").}
}
\value{
A \code{ggplot2} object showing the fit comparison.
}
\description{
Plots the observed relative frequencies of the responses against the theoretical probability distributions estimated by the MLCCUB model.
}
\examples{
\dontrun{
set.seed(123)
data_mock <- matrix(sample(1:5, 100 * 4, replace = TRUE), ncol = 4)
m_vec <- rep(5, 4)
model <- fit_MLCCUB(data_mock, m = m_vec, K = 2, EM_iter = 1, max_iter = 20)

# Goodness-of-fit plot: theoretical probabilities vs observed frequencies
p_fit <- plot_CUB_fit(model, data = data_mock, m = m_vec)
print(p_fit)
}
}
